const DisplayForObj = ({ name, subList }) => {
  return (
    <div>
      {/* <h1>Display</h1> */}
      <div>
        <h5>{name}</h5>
        <p>{subList}</p>
      </div>
    </div>
  );
};
export default DisplayForObj;
